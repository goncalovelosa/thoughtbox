name: thoughtbox-improvement
version: "1.0"

validation:
  min_runs: 3
  cv_threshold: 0.05  # 5%

budgets:
  per_run_tokens: 2000000      # ~$4 equivalent
  daily_tokens: 10000000       # ~$20 equivalent
